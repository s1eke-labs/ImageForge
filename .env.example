# ImageForge 环境变量配置
# 复制此文件为 .env 并修改为实际值：cp .env.example .env

# ======== 必填 ========

# JWT 签名密钥（必填，建议使用随机生成的长字符串）
# 生成方式: openssl rand -hex 32
IMAGEFORGE_JWT_SECRET=

# ======== 可选 ========

# 数据目录（宿主机路径），容器内挂载到 /data
# 包含 SQLite 数据库、上传的参考图和生成的结果图
DATA_DIR=./data

# 宿主机映射端口
PORT=8020
